app:
  name: authservice
http:
  mode: release
  port: 28080
db:
  url: lianmidba:12345678@tcp(mariadb:3306)/lianmicloud?charset=utf8&parseTime=True&loc=Local
  debug: false  
kafka:
  broker: kafka
  group: im
  Topics: Auth.Backend
redis:
  addr: redis:6379
  password: 
  db: 0
log:
  filename: /tmp/authservice.log
  maxSize: 500
  maxBackups: 3
  maxAge: 3
  level: "debug"
  stdout: true
consul:
  addr: consul:8500
jaeger:
  serviceName: authservice
  reporter:
    localAgentHostPort: "jaeger-agent:6831"
  sampler:
    type: const
    param: 1
