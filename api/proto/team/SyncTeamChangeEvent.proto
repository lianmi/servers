syntax = "proto3";
import "api/proto/team/TeamChange.proto";

package cloud.lianmi.im.team;
option go_package = "github.com/lianmi/servers/api/proto/team";

//2.5.25. 同步群组系统事件
//同步群组用户离线期间产生的离线事件
message SyncTeamChangeEvent{
   //本次同步服务器结束时间，unix时间戳，单位ms
   //是否必须-是
   fixed64 timeTag=1;
   //事件列表
   //是否必须-是
   repeated TeamChangeEvent events=2;
}