syntax = "proto3"; 
package cloud.lianmi.im.syn;
option go_package = "github.com/lianmi/servers/api/proto/syn";

//同步结束事件
//登录发送了sync操作指令后,所有sync相关的指令发送完成后
message SyncDoneEventRsp {
   //unix时间戳
   //是否必填-是
   fixed64 timeTag=1; 
}
 






